import { AppShell } from "@/components/app-shell";
import { MetricCard } from "@/components/metric-card";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
export default function AnalyticsPage() { return <AppShell><div className="mb-6"><h1 className="text-3xl font-black">Analytics</h1><p className="text-muted-foreground">Track clicks, saves, impressions, CTR, and best performing pins.</p></div><div className="grid gap-4 md:grid-cols-4"><MetricCard title="Clicks" value={4200} /><MetricCard title="Saves" value={1180} /><MetricCard title="Impressions" value={84200} /><MetricCard title="Outbound CTR" value={5} /></div><Card className="mt-6"><CardHeader><CardTitle>Best Performing Pins</CardTitle></CardHeader><CardContent><div className="space-y-3">{["Outfits That Look Expensive", "Sneakers Every Guy Needs", "Old Money Outfit Ideas"].map((title) => <div key={title} className="flex items-center justify-between rounded-md bg-muted p-3"><span className="font-medium">{title}</span><span className="text-sm text-muted-foreground">High CTR</span></div>)}</div></CardContent></Card></AppShell>; }
